// Project imports:
import '../../../../core/success/success-interface.dart';

class RequestInstantContactSuccess extends Success {
  const RequestInstantContactSuccess()
      : super(message: 'Request recieved, We will get back to you shortly');
}
